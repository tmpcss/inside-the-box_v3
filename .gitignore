# Dependencies
node_modules/

# Build output
dist/

# Environment variables (NEVER commit these)
.env
.env.local
.env.*.local
.env.production
.env.development
*.env

# Secrets / keys (extra safety net)
*.key
*.pem
*.p12
*.pfx
secrets.json
credentials.json
serviceAccount*.json

# Editor / OS
.DS_Store
.DS_Store?
Thumbs.db
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local settings (contains shell permissions, not keys — but keep private)
.claude/
